{
  "name": "universal-mean-api",
  "version": "0.6.0",
  "description": "NodeJs-Express-MongoDB-Universal-CRUD-API(Universal MEAN API) for dynamic MongoDB tables, no need to repeat the CRUD API code for every table. Focus on run it in a serverless environment.",
  "main": "./app/express/universal.app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "AlexStack",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-resource": "^1.0.0",
    "ini": "^2.0.0",
    "mongoose": "^5.10.8",
    "serverless-http": "^2.6.0"
  },
  "engines": {
    "node": "12.x"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  }
}
